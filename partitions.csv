# Partition table for a 4 MB ESP32-C6.
#
# Sized from measurement, not guesswork: the milestone-1 image is ~1.91 MiB of
# loadable segments (`readelf -lW` on the release ELF), so `factory` is given all
# the room that is left rather than the 1 MiB an espflash default would hand it.
#
# `nvs` is 64 KiB rather than the default 24 KiB because it holds *everything*
# persistent: rs-matter's fabrics/ACLs/sessions, the Thread credentials, the
# OpenThread SRP key, and this firmware's own two vendor blobs - all through
# `sequential-storage`, which is log-structured and needs slack to garbage-collect.
# `src/main.rs`'s `get_persistent_store` finds this partition by type at runtime, so
# resizing it needs no code change.
#
# App partitions must start on a 64 KiB boundary, hence `factory` at 0x20000.
#
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x10000,
phy_init,   data, phy,     0x19000,  0x1000,
factory,    app,  factory, 0x20000,  0x3E0000,
