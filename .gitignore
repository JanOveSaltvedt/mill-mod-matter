target/
# Per-board overrides of `config.toml`, so a differently-rated unit builds from a
# clean tree. See the header of `config.toml`.
config.local.toml

# Generated by `build.rs` from `config.toml`: the QR and manual pairing codes.
commissioning/
